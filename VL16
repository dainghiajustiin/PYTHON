def gcd(a, b):
    if(b == 0): return a 
    return gcd(b, a%b)

def init():

    return

def solve():
    init()

    a, b = list(map(int, input().split()))
    a = abs(a)
    b = abs(b)

    print(a*b//gcd(a, b))

    return

solve()
